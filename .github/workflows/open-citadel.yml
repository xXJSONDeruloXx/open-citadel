name: Open Citadel

concurrency:
  group: open-citadel-${{ github.ref }}
  cancel-in-progress: true

on:
  push:
    branches:
      - main
  pull_request:
    paths:
      - "OPEN_CITADEL.md"
      - "Makefile"
      - "tools/open_citadel_import.py"
      - "tests/test_open_citadel_import.py"
      - "tools/windows/**"
      - "open_citadel/**"
      - "android/**"
      - "loader/**"
      - "jni/**"
      - "src/**"
      - "thunks/**"
      - ".github/workflows/open-citadel.yml"
  workflow_dispatch:

jobs:
  donor-tools:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Python unit tests
        run: python3 -m unittest -v tests/test_open_citadel_import.py
      - name: Syntax check
        run: python3 -m py_compile tools/open_citadel_import.py tests/test_open_citadel_import.py

  windows-host-memory:
    runs-on: windows-2022
    steps:
      - uses: actions/checkout@v4
      - name: Configure 32-bit Windows test
        run: cmake -S tools/windows -B build/windows-host-memory -G "Visual Studio 17 2022" -A Win32
      - name: Build 32-bit Windows test
        run: cmake --build build/windows-host-memory --config Release --parallel
      - name: Run Windows memory test
        run: ctest --test-dir build/windows-host-memory -C Release --output-on-failure

  atc-decoder:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Build ATC decoder regression test
        run: g++ -std=c++20 -O2 -Wall -Wextra -Isrc tools/atc_color_test.cpp src/atc_decompress.cpp -o /tmp/atc_color_test
      - name: Run ATC decoder regression test
        run: /tmp/atc_color_test
      - name: Build ATC alpha regression test
        run: g++ -std=c++20 -O2 -Wall -Wextra -Isrc tools/atc_alpha_test.cpp src/atc_decompress.cpp -o /tmp/atc_alpha_test
      - name: Run ATC alpha regression test
        run: /tmp/atc_alpha_test

  x86-build:
    runs-on: ubuntu-24.04
    steps:
      - uses: actions/checkout@v4
      - name: Enable i386
        run: sudo dpkg --add-architecture i386
      - name: Install 32-bit build/runtime dependencies
        run: |
          sudo apt-get update
          sudo apt-get install -y g++-multilib libc6-dev-i386 libsdl2-dev:i386 zlib1g-dev:i386 libbsd-dev:i386 qemu-user
      - name: Verify 32-bit SDL
        env:
          PKG_CONFIG_LIBDIR: /usr/lib/i386-linux-gnu/pkgconfig:/usr/share/pkgconfig
        run: |
          printf '#include <SDL2/SDL.h>\nint main(){return 0;}\n' > /tmp/probe.cpp
          g++ -m32 /tmp/probe.cpp $(pkg-config --cflags --libs sdl2) -o /tmp/probe32
          file /tmp/probe32
      - name: Build Open Citadel i386 host
        env:
          PKG_CONFIG_LIBDIR: /usr/lib/i386-linux-gnu/pkgconfig:/usr/share/pkgconfig
        run: |
          make open-citadel-x86 OC_PKG_CONFIG=pkg-config
          file build/open-citadel-x86
          ldd build/open-citadel-x86
      - name: Smoke-test i386 executable
        run: |
          set +e
          output=$(build/open-citadel-x86 2>&1)
          rc=$?
          set -e
          echo "$output"
          test "$rc" -eq 2
          grep -F "usage:" <<<"$output"
      - name: Stage portable i386 runtime
        run: |
          set -eux
          out=build/open-citadel-x86-bundle
          mkdir -p "$out/lib" "$out/dri"
          cp build/open-citadel-x86 "$out/"
          cp -L /lib/ld-linux.so.2 "$out/ld-linux.so.2"

          # SDL loads EGL/GLES and Mesa's DRI driver dynamically, so ldd on
          # the executable alone is not enough. Seed those objects explicitly,
          # then recursively collect every absolute dependency they report.
          seeds="build/open-citadel-x86 /usr/lib/i386-linux-gnu/libEGL.so.1 /usr/lib/i386-linux-gnu/libGLESv2.so.2 /usr/lib/i386-linux-gnu/libGL.so.1 /usr/lib/i386-linux-gnu/libEGL_mesa.so.0 /usr/lib/i386-linux-gnu/libGLX_mesa.so.0"
          if [ -f /usr/lib/i386-linux-gnu/dri/swrast_dri.so ]; then
            cp -L /usr/lib/i386-linux-gnu/dri/swrast_dri.so "$out/dri/"
            seeds="$seeds /usr/lib/i386-linux-gnu/dri/swrast_dri.so"
          fi

          pending="$seeds"
          seen=""
          while [ -n "$pending" ]; do
            next=""
            for obj in $pending; do
              case " $seen " in *" $obj "*) continue ;; esac
              seen="$seen $obj"
              [ -e "$obj" ] || continue
              case "$obj" in
                /usr/lib/i386-linux-gnu/dri/*)
                  ;;
                /usr/lib/i386-linux-gnu/*|/lib/i386-linux-gnu/*|/lib32/*)
                  cp -Lf "$obj" "$out/lib/$(basename "$obj")"
                  ;;
              esac
              for dep in $(ldd "$obj" 2>/dev/null | awk '/=> \/|^\// {for(i=1;i<=NF;i++) if($i ~ /^\//) print $i}'); do
                case " $seen " in *" $dep "*) ;; *) next="$next $dep" ;; esac
              done
            done
            pending="$next"
          done

          cp -L /usr/lib/i386-linux-gnu/libEGL.so.1 "$out/lib/"
          cp -L /usr/lib/i386-linux-gnu/libGLESv2.so.2 "$out/lib/"
          cp -L /usr/lib/i386-linux-gnu/libGL.so.1 "$out/lib/"
          cp -L /usr/lib/i386-linux-gnu/libEGL_mesa.so.0 "$out/lib/"
          cp -L /usr/lib/i386-linux-gnu/libGLX_mesa.so.0 "$out/lib/"
          mkdir -p "$out/glvnd/egl_vendor.d"
          cp /usr/share/glvnd/egl_vendor.d/50_mesa.json "$out/glvnd/egl_vendor.d/"

          # The ChatGPT execution sandbox used for private-donor testing has
          # an x86_64 kernel with IA32 execution disabled. Package qemu-i386
          # and its host-side dependencies so the exact CI-built guest host can
          # still be exercised locally without publishing Epic's donor.
          qemu="$(command -v qemu-i386)"
          mkdir -p "$out/qemu-lib"
          cp -L "$qemu" "$out/qemu-i386"
          pending="$qemu"
          seen=""
          while [ -n "$pending" ]; do
            next=""
            for obj in $pending; do
              case " $seen " in *" $obj "*) continue ;; esac
              seen="$seen $obj"
              for dep in $(ldd "$obj" 2>/dev/null | awk '/=> \/|^\// {for(i=1;i<=NF;i++) if($i ~ /^\//) print $i}'); do
                case "$dep" in
                  /lib/x86_64-linux-gnu/*|/usr/lib/x86_64-linux-gnu/*)
                    cp -Lf "$dep" "$out/qemu-lib/$(basename "$dep")"
                    ;;
                esac
                case " $seen " in *" $dep "*) ;; *) next="$next $dep" ;; esac
              done
            done
            pending="$next"
          done

          file "$out/open-citadel-x86" "$out/ld-linux.so.2" "$out/qemu-i386"
          du -sh "$out"
      - name: Upload portable i386 host
        uses: actions/upload-artifact@v4
        with:
          name: open-citadel-x86-${{ github.sha }}
          path: build/open-citadel-x86-bundle
          retention-days: 7
