name: Probe Common Crawl HTML5 payloads

on:
  push:
    branches:
      - feat/html5-revival
    paths:
      - "tools/html5_commoncrawl_probe.py"
      - "tools/html5_wayback_probe.py"
      - ".github/workflows/html5-commoncrawl-probe.yml"
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: html5-commoncrawl-probe
  cancel-in-progress: true

jobs:
  probe:
    runs-on: ubuntu-latest
    timeout-minutes: 8
    steps:
      - uses: actions/checkout@v4
      - name: Fingerprint archived loader metadata
        run: python3 tools/html5_wayback_probe.py
      - name: Probe Common Crawl
        run: python3 tools/html5_commoncrawl_probe.py
      - name: Print Common Crawl report
        if: always()
        run: |
          if [ -f html5-commoncrawl-probe.json ]; then
            python3 -m json.tool html5-commoncrawl-probe.json
          fi
